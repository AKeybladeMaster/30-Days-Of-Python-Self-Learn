# Exercises - Day 24

from statistics import mean, median, mode, pvariance, pstdev

data = [19, 22, 19, 24, 20, 25, 26, 24, 25, 24]
print({'mean': mean(data), 'median': median(data), 'mode': mode(data),
       'variance': pvariance(data), 'standard_deviation': pstdev(data)})


# SKIPPING REPEATING ALL THE EXAMPLES FOR TIME REASONS